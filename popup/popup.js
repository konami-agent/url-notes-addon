const currentUrl = document.querySelector('#current-url');
const noteKey = document.querySelector('#note-key');
const saveStatus = document.querySelector('#save-status');

currentUrl.textContent = 'URL loading will be implemented in T4.';
noteKey.textContent = 'Key loading will be implemented after the core library is ready.';
saveStatus.textContent = 'Scaffold ready.';
